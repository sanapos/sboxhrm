import 'package:flutter/material.dart';

Widget buildPosHtmlPreview(String htmlDocument) => const SizedBox(
      child: Center(child: Text('Xem trước HTML chỉ hỗ trợ trên web')),
    );

Future<void> printPosHtmlDocument(String htmlDocument) async {}
