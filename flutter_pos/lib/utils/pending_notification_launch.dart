class PendingNotificationLaunch {
  static Future<void> consume({
    bool adminPortalMode = false,
    bool agentMode = false,
  }) async {}
  static Future<void> scheduleConsume({
    bool adminPortalMode = false,
    bool agentMode = false,
  }) async {}
}
